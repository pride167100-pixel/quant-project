# 퀀트 스크리너 프로젝트 - 다음 단계 요구사항 정리

## 프로젝트 현황

- 국내주식(코스피/코스닥) 퀀트 스크리너 + 모의매매 + 백테스터
- 기술스택: Python, Streamlit, SQLite, 한국투자증권(KIS) API, DART(전자공시) API
- GitHub: (레포 clone 후 시작)
- 지금까지 완성된 것: 스크리너(필터+랭킹 모드), 모의매매(매수/매도/거래비용/벤치마크비교/추가입금), 백테스터(필터+랭킹모드, 분기별 정밀 재무데이터 반영, 벡터화로 속도개선), 로그인/API설정 화면, 모의매매/실전매매 모드선택(실전은 "준비중" 상태)
- 배포: 임베드 파이썬으로 패키징한 로컬 실행판 1개 완성 (`quant-deploy`, 참고용. 이번 라운드에서 서버 배포로 전환하며 재검토 필요)

## ⚠️ 0순위 - 가장 먼저 진행 (다른 작업보다 최우선)

### 백테스터 수익률 이상 현상 진단 및 수정
**증상**: 여러 프리셋을 36개월 백테스트하면 매년 7/15~8/15 구간에서 공통적으로 -99% 수준으로 수익률이 급락함. 반면 같은 프리셋의 실제 모의매매(최근 2주)는 +33% 수준의 정상적인 수익률을 보임. 특정 계절 구간에서만 반복적으로 발생하는 패턴이라 백테스터 계산 로직 또는 데이터 자체의 결함으로 추정됨.

**확인해볼 만한 방향** (추측이며 확인 필요):
- 이 구간에 액면분할/무상증자 등 주가 급변 이벤트가 있었는데 보정이 안 된 종목이 있는지
- 과거 주가 데이터(historical_prices.csv) 수집 시 이 구간에서만 데이터 공백/이상치가 있는지
- 재무데이터(financial_timeline.csv) 조회가 이 시즌에 실패해서 극단적인 PBR/PER이 계산되는 경우가 있는지
- 백테스터의 리밸런싱 로직에서 이 시점에만 발생하는 엣지 케이스가 있는지

**이 작업이 최우선인 이유**: 이후 다른 기능(다중 프리셋 비교, 결과 출력 등)을 아무리 잘 만들어도, 기반이 되는 백테스터 계산이 틀렸다면 전부 신뢰할 수 없는 결과를 예쁘게 포장하는 것에 불과함.

**진단에 도움이 되는 것**: 아래 "1순위 - 결과 출력/투명성" 기능을 먼저(또는 동시에) 만들면, 그 시점에 실제로 어떤 종목을 들고 있었고 어떤 가격으로 계산됐는지 확인할 수 있어 원인 파악이 쉬워짐.

---

## 1순위 - 신뢰성/투명성 확보 (다른 기능의 기반이 됨)

### 1-1. 결과 출력 기능
- 프리셋을 체크(다중 선택 가능)하고 "출력" 버튼 → 조건(필터 수치, 랭킹 설정), 매매 시작일, 현재 수익률, 보유 종목, 거래 이력을 텍스트(프롬프트 형태)로 정리해서 보여줌
- 복사해서 AI(Claude, Gemini 등)에게 붙여넣어 상담하기 좋은 포맷으로
- 백테스터 결과도 동일한 방식으로 출력 지원 (종목 수 비교 모드의 N별 결과값도 포함)
- 모의매매의 매수/매도 로그(거래 이력)가 현재 별도로 조회 가능한 형태로 존재하지 않는다면, 이 기능을 위해 먼저 로그 저장 구조부터 확인/보강 필요

### 1-2. 리밸런싱 시스템화
- 현재는 필터/랭킹으로 뽑은 종목을 "매수"만 할 수 있고, 조건을 벗어난 보유 종목을 자동으로 찾아 매도하는 기능이 없음
- "리밸런싱" 버튼 신설: 보유 종목 중 조건 미충족 종목 매도 + 새로 조건 충족한 종목 매수 (백테스터의 리밸런싱 로직을 실제 모의매매에도 적용하는 개념)
- 리밸런싱 주기 자동 설정(예: 매달) 옵션, 또는 수동 리밸런싱 시 "마지막 리밸런싱 날짜"와 "경과일" 기록/표시
- 프리셋 비교 화면에서도 각 프리셋이 실제 어떤 종목을 보유 중인지 확인 가능하게 (지금은 수익률 숫자만 보이고 내역이 안 보여서 신뢰도가 낮다는 피드백 있었음)

---

## 2순위 - 사용성 개선

- 프리셋별 첫 매매 시작일(실제 첫 매수 시점) 화면 표시
- "프리셋 비교" 화면 진입 시 스냅샷 상태인지 최신 상태인지 더 명확히 표시 (자동 새로고침 또는 시각 강조)
- 프리셋 이름 수정 기능 개선 (현재 변경 불가 또는 매우 불편함)
- 균등분배 매수 시 잔금 과다 문제: 1,000만원 투자해도 30~50만원씩 남는 경우 있음. "1인당 배분액 ÷ 가격"으로 단순히 끊어 사는 대신, 남는 현금을 재배분하는 방식으로 개선하여 잔금을 최소화

---

## 3순위 - 신규 기능

### 3-1. 프리셋 수익률 시계열 그래프
- 매수 시점부터 현재까지, 매일 장 종료 가격 기준의 수익률을 주식 차트처럼 시계열로 기록
- 여러 프리셋의 수익률 추이를 겹쳐서 비교하는 그래프 ("A가 앞서다 B가 역전" 등을 한눈에, 시장 상황별 강세 프리셋 파악 목적)
- 이를 위해 "매일 장마감 시점 가격을 기록해서 쌓아두는" 구조가 새로 필요할 수 있음

### 3-2. 백테스터 다중 프리셋 비교
- 현재는 한 번에 프리셋 하나만 백테스트 가능
- 여러 프리셋을 동시에 실행하거나, 결과를 저장해뒀다가 나중에 서로 비교 가능하게

### 3-3. 랭킹모드 "순위 단순 합산" 방식 개선 (설계 논의 필요)
- 현재는 여러 지표의 순위를 단순 합산 (예: A지표 10등+B지표 2등 = 12점, A지표 8등+B지표 4등 = 12점, 이 둘을 동일하게 취급)
- 문제: "한 지표에 몰빵되어 강함"과 "여러 지표에 고르게 준수함"이 구분되지 않음
- 가중치 부여, 편차에 대한 페널티 등 균형도를 반영하는 대안 검토 후 구현

### 3-4. 전체 UI/UX 정리
- 기능이 많이 쌓여서(모의매매/프리셋/비교/벤치마크/백테스터) 화면 구조 재정리 필요
- "현황 요약" 버튼 신설: 현재 프리셋의 보유종목 분석 + 거래내역 + 매수/매도 사유를 종합 정리해서 보여줌 (AI에게 평가받고 필터 조정 논의하기 편하게). 1-1번 "결과 출력" 기능과 성격이 겹치므로, 하나로 통합할지 검토 후 결정

---

## 4순위 - 인프라 (아래 참고: 이번 라운드에서 필수 요건으로 격상됨)

### 서버 구축 + 실전매매 연동
아래 요구사항들이 종합되어, 서버 구축이 "나중에 할 수도 있는 선택사항"이 아니라 **이번 개발 사이클의 핵심 목표**가 되었음:

- 컴퓨터/모바일 어디서든, 같은 와이파이가 아니어도 접속 가능해야 함
- 한쪽 기기(예: 컴퓨터)에서 프리셋을 설정/적용하면, 다른 기기(다른 컴퓨터, 모바일)에서도 즉시 이어져서 보여야 함 (= 데이터가 로컬 파일이 아니라 서버의 중앙 저장소에 있어야 함)
- 실전매매(KIS 실제 주문 API 연동)까지 지원해야 함
- 사용자 규모: 본인 위주, 많아야 1~2명 정도로 소규모. 고사양 서버는 불필요, 가벼운 서버로 충분

**고려해야 할 점**:
- 여러 기기에서 같은 데이터를 보려면 SQLite 로컬 파일 구조를 서버의 중앙 DB로 전환 필요 (또는 최소한 서버에서 SQLite를 호스팅하고 각 기기는 그 서버에만 접속)
- API 키(KIS, DART)를 서버에 안전하게 보관하는 방법 설계 (지금처럼 로컬 .env가 아니라 서버 환경에서의 보안 저장 방식 필요)
- 실전매매 연동 시 주문 오류/중복 주문 등에 대한 안전장치 설계
- 서버 후보: 오라클 클라우드 Always Free(무료지만 가입 절차가 까다로울 수 있음), 국내/해외 저가형 VPS(월 5천~1만원대) 중 선택. 최신 요금제는 신청 전 직접 확인 필요
- 지금의 Streamlit 웹앱 구조는 그대로 유지 (네이티브 모바일 앱으로 재개발하지 않음 - 브라우저 접속만으로 컴퓨터/모바일 둘 다 대응 가능하므로 새로 앱을 만들 필요 없음)

---

## 명시적으로 보류/제외하기로 한 것

- **AI 자동 시황요약 → 자동 리밸런싱 판단**: 논의 결과, 시스템의 "재현 가능성/백테스트 검증 가능성"과 충돌하여 지양하기로 함. AI 요약은 어디까지나 "참고자료"로만 활용하고, 최종 리밸런싱 판단은 사람이 하는 구조를 유지. 필요시 별도 스크립트로 자문 AI에게 넘기는 방식은 나중에 검토 가능하나 이번 범위에는 포함하지 않음.
- **네이티브 모바일 앱 개발**: 지금의 Streamlit 웹앱 구조를 유지하고, 서버 배포로 모바일 접근성을 해결. Flutter/React Native 등으로 처음부터 다시 만들지 않음.
- **생존편향 해소, 분기 단위 정밀화(현재는 연간 근사 → 이미 일부 정밀화됨)**: 이전 라운드에서 보류 결정된 항목들, 이번 라운드 범위 밖 (필요시 추후 별도 요청)

